global:
  namespace: staging
  dockeruser: tlavu2004

imageTag: &tag 3.0

services:
  api-gateway:
    image:
      tag: *tag
    ingress:
      host: petclinic-staging.com

  admin-server:
    image:
      tag: *tag

  config-server:
    image:
      tag: *tag

  discovery-server:
    image:
      tag: *tag

  customers-service:
    image:
      tag: *tag
    
  vets-service:
    image:
      tag: *tag

  visits-service:
    image:
      tag: *tag

  genai-service:
    image:
      tag: *tag
